# Production build — uses multi-stage Dockerfile
# Usage: docker compose -f docker-compose.prod.yml up --build
services:
  mixtape:
    build: .
    ports:
      - '3001:3001'
    env_file: .env
    environment:
      - NODE_ENV=production
      - PORT=3001
